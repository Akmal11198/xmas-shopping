import { ListContent } from "./ListContent";
import { ListToggle } from "./ListToggle";

export const MainList = () => {
  return (
    <div id="column">
    <ListToggle />
    <ListContent />
  </div>
  );
};
